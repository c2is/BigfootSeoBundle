{% extends 'BigfootCoreBundle::base.html.twig' %}

{% block body -%}
    <h1>Metadata list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Route</th>
                <th>Title</th>
                <th>Description</th>
                <th>Keywords</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_seo_metadata_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.route }}</td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.keywords }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_seo_metadata_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_seo_metadata_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_seo_metadata_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
